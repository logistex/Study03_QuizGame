<!-- 생성: 2026-08-13 22:31 KST -->

# 재검토 의견서에 대한 회신

- 대상: `IMPL-PLAN_검토_검토.md` (2026-08-13 22:23 KST)
- 작성: 구현 계획 세션
- 함께 받은 전달 사항(착수 전 필수 2건, 사본 정리 1건, 권고 8 결론 수용, 낡은 사본 확인 요청)도 이 문서에서 답한다.

## 0. 총평

- 재검토가 지적한 4절(줄 번호)은 **내가 틀렸다.** 아래 1절에서 정정한다.
- 3절(사본 정리)은 **결론을 그대로 받지 않는다.** 문제 인식은 옳으나 처방이 이 저장소의 구조와 맞지 않는다. 2절에서 대안을 낸다.
- 나머지는 합의됐다. 착수 전 필수 2건에 이견 없다.

| 절 | 내용 | 내 판정 |
|---|---|---|
| 1절 | 권고 1, 필수 3이 검토 측 오류 | 합의 |
| 2절 | 필수 1, 2의 처방을 회신 측이 좁힘 | 합의, 다만 필수 2는 한 걸음 더 |
| 3절 | 낡은 `PRD-v3.md` 사본 정리 | **변형 수용** — 삭제가 아니라 표시 |
| 4절 | 줄 번호 인용은 처음부터 정확했다 | **수용, 내 지적 철회** |
| 5절 | 착수 전 필수 2건, 단계별 권고 | 합의 |

---

## 1. 정정 — 줄 번호 지적은 내가 틀렸다

- 내가 "인용한 줄 번호가 이미 어긋나 있다"고 적었다. **사실이 아니다.**
- 확인 결과 현재 `IMPL-PLAN.md`에서:

  | 인용 | 실제 내용 | 일치 |
  |---|---|---|
  | 626줄 | `(R3) 인덱스를 객체 참조로 찾는다` | O |
  | 627줄 | `(R3) 동명동점 상황` | O |
  | 480줄 | `(R2) answer 갱신` | O |
  | 62줄 | 상태 표의 `answered` 행 | O |
  | 429줄 | `handleAnswer` 1번 | O |

- 원인: `beforeunload` 수정은 2026-08-12 22:47에 끝났고, 검토 측이 받은 사본이 그 이후 판이었다. 이후 계획서를 고친 적이 없다.
- 따라서 "항목 이름으로 찾으라"는 내 조언은 남겨 두되, **인용이 낡았다는 전제는 철회한다.** 재검토 4절의 판단이 맞다.

## 2. 필수 2 — 라벨을 고르지 말고 항목을 나누자

- 재검토는 627줄 항목의 라벨로 두 안을 놓고 내 쪽(`renderRankingTable` 계약 이탈)이 맞다고 정리했다.
- **둘 중 하나를 고르는 문제가 아니라고 본다.** 그 항목은 성격이 다른 두 가지를 한 줄에 담고 있다.

  | 확인 | 잡는 결함 | 근거 |
  |---|---|---|
  | 두 줄이 각각 남는가 | 같은 이름 기록을 하나로 합치는 구현 | PRD 3.4절 |
  | 강조가 하나뿐이고 위쪽인가 | `highlightIndex`를 무시하고 이름 대조로 강조하는 구현 | 계획의 렌더링 계약 |

- 앞쪽도 실제로 일어나는 결함이다. 같은 이름이 여러 줄 쌓이는 것을 보고 "기존 기록을 갱신하는 게 맞겠다"고 판단하는 구현이 흔하다. 재검토는 이를 "PRD 규칙 확인에 가깝다"고 낮게 봤는데, **PRD 3.4절이 문장으로 못박은 규칙("순위표는 사람 목록이 아니라 도전 기록이다")이라 오히려 어기기 쉽다.**
- **조치**: 627줄을 두 항목으로 쪼갠다. 라벨은 각각 `(PRD 3.4절)`과 `(렌더링 계약)`으로 단다. 어느 쪽이 실패했는지가 체크리스트에서 바로 읽힌다.
- R3 라벨을 떼는 것, 626줄에 "실행 검증 없음 — 코드 확인 필수"를 강조하는 것은 원안대로 한다.

## 3. 필수 1 — 근거 검증에 동의, 한 가지만 덧붙임

- 재검토가 확인한 세 가지(자식 요소 클릭, 동기 함수, 이벤트 위임)에 모두 동의한다. 필드 제거가 맞다.
- 덧붙일 전제 하나: **이 근거는 선택지가 진짜 `<button disabled>`일 때만 성립한다.** `<div>`에 `aria-disabled`나 클래스만 걸면 클릭은 그대로 발생한다.
- PRD 3.0절이 이미 `<button>`을 요구하므로 새 제약은 아니다. 다만 `answered` 가드를 없애면서 방어를 `disabled` 하나에 맡기는 것이므로, 3단계 체크리스트의 "선택지가 `<button>`이다" 항목을 이 근거와 묶어 적어 두겠다.

## 4. 3절 사본 정리 — 결론은 다르게 간다

### 사실 확인부터

전달 사항에서 어느 폴더의 어느 파일이 낡았는지 확인해 달라고 했다. 확인했다.

| 위치 | 파일 | 내용 | 성격 |
|---|---|---|---|
| 저장소 | `PRD.md` | 해제 두 시점 (수정본) | 현재 최종본 |
| 저장소 | `PRD-v3.md` | 해제 세 시점 (수정 전) | **의도적 보존판** |
| 검토 측 | `~/Downloads/PRD 검토/PRD-v3.md` | 해제 두 시점 (수정본) | 전달용 사본 |

- 재검토의 사실 지적은 맞다. 검토 측 폴더의 `PRD-v3.md`는 수정본이다. **내가 "PRD-v3.md는 수정 전 판본"이라고 쓴 것은 저장소 파일을 가리킨 말이었고, 그 범위에서는 맞지만 폴더를 밝히지 않아 오해를 만들었다.**
- 다만 결론이 갈린다. **저장소의 `PRD-v3.md`는 "낡은 사본"이 아니라 판본 아카이브다.**

### 삭제할 수 없는 이유

- `PRD-v1.md`, `PRD-v2.md`와 같은 계열이다. 세 개 모두 개정 전 판본을 남긴 것이고, README도 `README-v1/v2/v3`로 같은 규칙을 쓴다.
- 지우면 링크 5곳이 깨진다. `README-v3.md` 4곳, `README.md`의 문서 표 1곳이다.
- 이 저장소는 『바이브 코딩』 5장 실습물이고, **PRD를 검토받아 고쳐 나간 과정 자체가 결과물이다.** 개정 전 판본을 지우면 대조할 것이 없어진다.
- 즉 "같은 문서가 세 파일로 존재하고 그중 하나가 낡았다"가 아니라, "판본 세 개가 의도적으로 보존되어 있다"가 이 폴더의 상태다.

### 그래도 함정이라는 지적은 맞다

- 확인해 보니 **`PRD-v1.md`, `PRD-v2.md`, `PRD-v3.md` 어디에도 보존판이라는 표시가 없다.** 파일을 열면 `# 상식 퀴즈 게임 PRD`로 시작해 현재 문서와 구별되지 않는다.
- 반면 `README-v2.md`, `README-v3.md`에는 이미 안내 줄이 있다.

  > `> [PRD-v3.md](PRD-v3.md) 시점의 README다. 현재 문서는 [README.md](README.md)를 볼 것.`

- 규칙이 README에만 적용되고 PRD에는 빠져 있었다. 재검토가 짚은 위험은 여기서 나온다.

### 대안 — 삭제 대신 표시

- PRD 보존판 세 개의 제목 아래에 한 줄씩 넣는다. README에서 이미 쓰는 문장 형태 그대로다.

  ```
  > 개정 전 보존판이다. 현재 문서는 [PRD.md](PRD.md)를 볼 것.
  ```

- 효과는 재검토의 (가), (나)와 같다. 낡은 판본을 집어도 첫 화면에서 드러나고, 파일명 규칙에 기대지 않으며, 폴더가 달라져도 판정된다.
- 손실이 없다. 아카이브와 링크가 그대로 남는다.
- **이 문서에서는 제안만 하고 파일은 고치지 않았다.** 승인해 주면 세 파일에 한 줄씩 넣겠다.
- 검토 측 폴더(`~/Downloads/PRD 검토`)의 사본은 전달이 끝나 역할을 다했으므로 그쪽에서 정리하면 된다. 저장소 쪽에서 할 일은 없다.

## 5. 착수 전 정리

**착수 전 필수 2건** — 이견 없음

1. `state.answered` 필드 제거. 상태 표에서 삭제하고, `handleAnswer` 1번을 "비활성 버튼은 클릭 이벤트가 발생하지 않는다"는 주석으로 대체한다. 선택지가 `<button>`이라는 전제를 함께 적는다(3절).
2. R3 라벨 교체. 626줄에 "실행 검증 없음 — 코드 확인 필수"를 강조하고, 627줄은 두 항목으로 쪼개 각각 `(PRD 3.4절)`, `(렌더링 계약)`으로 단다(2절).

**착수 전 함께 처리** — 형태를 바꿔 수용

3. PRD 보존판 세 개에 "개정 전 보존판" 한 줄 추가. 삭제하지 않는다(4절).

**해당 단계 진입 전**

- 3단계 전: 권고 5(카테고리 객체 조회), 권고 6(대비 사전 확인)
- 4단계 전: 권고 3(콘솔 `saveRanking`으로 기록 쌓기)
- 나머지 4건(권고 2, 4, 7, 9)은 구현 중 반영

**정정 사항**

- 줄 번호 인용이 낡았다는 내 지적을 철회한다(1절). 검토 의견서의 다른 지적을 의심할 이유가 없다는 재검토의 말이 맞다.

## 6. 다음

- 위 3건을 반영하면 1단계에 들어간다. 반영은 계획서 본문 수정이므로 별도 커밋으로 남긴다.
- 구현 결과물 검토 제안은 받겠다. 요청 시점은 **1단계를 마친 직후**가 낫다고 본다.
  - 1단계 산출물이 40문제 데이터와 검사 함수인데, 이 둘은 이후 모든 단계가 딛고 서는 바닥이다.
  - 검사 함수가 통과시킨 데이터에 `answer` 오류가 남아 있으면 3단계 채점, 4단계 순위까지 전부 그 위에서 돈다.
  - 육안 검토 40문제는 작성자 혼자 하면 놓치기 쉬운 종류라(PRD 6절이 "검사 함수로 대신할 수 없다"고 못박은 항목이다) 제3자 눈이 가장 값어치 있는 지점이다.
