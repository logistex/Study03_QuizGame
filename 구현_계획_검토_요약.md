<!-- 생성: 2026-08-13 22:55 KST -->

# 구현 계획 검토 요약

`IMPL-PLAN.md`를 새 세션에서 검토받고 합의에 이르기까지의 경과를 한 문서로 정리했다. 원문은 저장소에 그대로 남아 있으며, 이 문서는 무엇이 오갔고 무엇으로 결론이 났는지만 간추린 것이다.

## 1. 개요

- **대상**: `IMPL-PLAN.md` (PRD 6절의 여섯 단계를 구현 순서로 옮긴 계획)
- **기간**: 2026-08-13 21:17 ~ 22:55
- **방식**: 계획을 쓴 세션과 검토 세션을 분리했다. 검토 세션은 계획서와 PRD만 받았고 저장소 상태는 알지 못했다.
- **결과**: 지적 12건 중 **11건 반영, 1건 반려.** 반려 건은 대안으로 대체했다.

검토 세션을 분리한 이유는 계획을 쓴 쪽이 자기 계획의 빈틈을 보기 어렵기 때문이다. 실제로 지적된 필수 3건 중 둘은 계획을 쓴 쪽이 스스로 찾지 못한 것이었다.

## 2. 오간 문서

| # | 문서 | 작성 | 요지 |
|---|---|---|---|
| 1 | [IMPL-PLAN_검토.md](IMPL-PLAN_검토.md) | 검토 측 | 1차 검토. 필수 3건, 권고 9건 |
| 2 | (회신 1) | 계획 측 | 12건 판정. 문서로 남기지 않고 대화로 전달했다. 내용은 3번 문서에 인용되어 있다 |
| 3 | [IMPL-PLAN_검토_검토.md](IMPL-PLAN_검토_검토.md) | 검토 측 | 회신 재검토. 판정을 되돌릴 항목 없음, 검토 측 오류 2건 인정 |
| 4 | [IMPL-PLAN_검토_검토_회신.md](IMPL-PLAN_검토_검토_회신.md) | 계획 측 | 최종 회신. 정정 1건, 대안 1건, 항목 분리 1건 |

관련 커밋: `789eedc` → `d7d7097` → `b0a2571` → `ff95ac5`(반영)

## 3. 1차 검토 의견

계획이 **PRD 요구사항을 빠뜨린 것은 없다**는 확인에서 출발했다. 지적은 전부 검증 절차와 함수 명세를 향했다.

**필수 3건**

| # | 지적 | 왜 문제인가 |
|---|---|---|
| 1 | `state.answered`를 `true`로 만드는 곳이 없다 | 가드가 늘 `false`를 읽는다. 실제 방어는 `disabled`가 하므로 체크리스트도 통과한다. 죽은 코드가 검증까지 통과한 채 남는다 |
| 2 | R3 체크리스트 두 항목이 서로 모순된다 | 한쪽은 "실행으로 판정되지 않는다"고 하고 다른 쪽은 실행으로 판정하라고 한다. 통과해도 R3를 검증한 것이 아닌데 검증했다고 기록에 남는다 |
| 3 | git 저장소를 만드는 시점이 없다 | 5단계 체크리스트가 `git diff`를 요구하는데 git은 6단계에 처음 등장한다 |

**권고 9건** — `renderResult`의 데이터 출처 미정의, R2 검증의 무효한 앞부분, 11판 수동 플레이, 호출 순서 미명시, 카테고리 객체 조회, 대비 확인 시점, 순위표 핸들러 소속 단계, 기준 문서명, GitHub Pages 캐시.

## 4. 협의 경과

주고받으며 결론이 바뀐 것만 적는다. 나머지 7건은 1차에서 그대로 수용됐다.

### 필수 1 — 두 안 중 무엇을 고를 것인가

- 검토 측은 "대입 추가 **또는** 필드 제거"를 나열하고 판단을 넘겼다.
- 계획 측이 **제거**를 골랐다. 선택지 버튼을 다시 활성화하는 경로가 없어 `disabled` 하나로 충분하다.
- 검토 측이 근거를 검증했다. 비활성 버튼은 자식 요소를 눌러도 클릭 이벤트가 발생하지 않고, `handleAnswer`가 동기 함수라 연타의 두 번째 클릭은 이미 비활성이 적용된 뒤에 도착한다.
- 계획 측이 전제 하나를 덧붙였다. **이 근거는 선택지가 진짜 `<button>`일 때만 성립한다.** `<div>`에 `aria-disabled`만 걸면 클릭이 그대로 발생한다.

### 필수 2 — 라벨을 바꿀 것인가, 항목을 나눌 것인가

| 라운드 | 주장 |
|---|---|
| 검토 측 | 문제의 항목을 `PRD 3.4절` 검증으로 라벨을 바꾸자 |
| 계획 측 | 그 항목이 잡는 것은 렌더링 계약 이탈이다 |
| 검토 측 | 계획 측 라벨이 맞다 |
| 계획 측 | **둘 중 하나를 고르는 문제가 아니다.** 성격이 다른 둘이 한 줄에 섞여 있다 |

최종적으로 항목을 **두 개로 쪼갰다.** 같은 이름 기록이 합쳐지지 않는가(PRD 3.4절)와 강조가 `highlightIndex`로만 걸리는가(렌더링 계약)는 실패했을 때 원인이 다르다.

### 필수 3 — 저장소는 이미 있었다

- 계획 측이 반박했다. 이 폴더가 곧 저장소이고 PRD와 계획이 이미 커밋되어 있어 `git init`은 틀린 지시가 된다.
- 검토 측이 수용했다. 계획 문서만으로는 알 수 없는 사실이었다.
- 다만 **`git diff`를 원상 복구 확인에 쓰라는 실질은 살렸다.** 1단계 검사 함수 검증이 데이터를 여덟 번 망가뜨렸다 되돌리는 절차라 눈 대조보다 확실하다.

### 권고 1 — PRD 계약을 지킬 것인가

- 검토 측은 `saveRanking`이 `{ index, list }`를 돌려주는 안을 선호했다.
- 계획 측이 거절했다. PRD 3.4절이 반환값을 정수, `null`, `'unavailable'` 세 가지로 못박았고 3.3절이 "저장 함수의 반환값으로 판단한다"고 적었다. 구현 편의로 PRD 계약을 조용히 바꾸는 셈이다.
- 검토 측이 수용했다. `renderResult`가 `readRankings()`를 다시 부르는 쪽으로 갔다.
- 이후 검토 측이 **이 항목이 실행 일정에서 빠진 것을 발견해** 다시 넣었다.

### 권고 8 — 반려, 그리고 대안

유일한 반려 건이다. 결론에 이르는 데 세 라운드가 걸렸다.

| 라운드 | 내용 |
|---|---|
| 검토 측 | 계획서가 기준 문서를 `PRD.md`라 적었는데 `PRD-v3.md`로 고쳐야 한다 |
| 계획 측 | **반려.** 이 폴더에서는 `PRD.md`가 최신본이다. 고치면 구버전을 가리킨다 |
| 검토 측 | 반려 수용. 다만 낡은 `PRD-v3.md` 사본을 지우자 |
| 계획 측 | **그것도 받지 않는다.** 저장소의 `PRD-v3.md`는 낡은 사본이 아니라 판본 아카이브다 |

두 세션의 폴더에 같은 이름의 다른 파일이 있어 생긴 어긋남이었다. 검토 측 폴더의 `PRD-v3.md`는 수정본이고, 저장소의 `PRD-v3.md`는 `PRD-v1/v2`와 같은 계열의 보존판이다. 지우면 `README-v3.md` 4곳과 `README.md` 1곳의 링크가 깨지고, 검토 과정을 대조할 근거가 없어진다.

**대안**: 삭제 대신 표시. `PRD-v1/v2/v3` 제목 아래에 한 줄씩 넣었다.

> 개정 전 판본을 보존한 것이며, 현재 문서는 [PRD.md](PRD.md)를 볼 것.

`README-v1/v2/v3`가 이미 쓰던 방식인데 PRD 쪽에만 빠져 있었다. 위험은 없애고 아카이브는 남겼다.

### 정정 — 계획 측이 틀린 것 1건

계획 측이 "검토 의견서의 줄 번호 인용이 이미 어긋나 있다"고 적었으나 사실이 아니었다. 확인 결과 인용한 다섯 곳이 현재 파일과 모두 일치했다. `beforeunload` 수정이 사본 전달보다 앞서 끝나 있었다. 지적을 철회했다.

## 5. 최종 합의

| # | 항목 | 판정 | 반영 형태 |
|---|---|---|---|
| 필수 1 | `state.answered` | 수용 | 필드 제거, `disabled` 하나로 방어. `<button>` 전제를 체크리스트에 명시 |
| 필수 2 | R3 체크리스트 | 수용 | 코드 확인 항목에 "실행 검증 없음" 명시, 나머지를 두 항목으로 분리 |
| 필수 3 | `git init` | **부분 수용** | `git init`은 불필요. `git diff`만 1단계에 반영 |
| 권고 1 | `renderResult` 데이터 출처 | 수용 | `readRankings()` 재호출. 저장 함수 계약은 유지 |
| 권고 2 | R2 검증 | 수용 | 무효한 앞부분 제거, 열 문제 일괄 대조로 교체 |
| 권고 3 | 11판 수동 플레이 | 수용 | 콘솔 `saveRanking` 호출로 대체 가능. 완료 기준 1번만 실제 플레이 |
| 권고 4 | 호출 순서 | 수용 | `showScreen` → `renderQuestion` 명시 |
| 권고 5 | 카테고리 객체 조회 | 수용 | `CATEGORIES.find(...)` 명시 |
| 권고 6 | 대비 확인 시점 | 수용 | 3단계 색 선정 시 사전 확인 추가 |
| 권고 7 | 순위표 핸들러 | 수용 | 2단계 이벤트 바인딩 몫임을 명시 |
| 권고 8 | 기준 문서명 | **반려** | 보존판 표시로 대체 |
| 권고 9 | Pages 캐시 | 수용 | 재배포 후 404가 이어지면 캐시를 먼저 배제 |

반영 커밋은 `ff95ac5`다. 계획서 외에 `PRD-v1/v2/v3`와 `README.md`가 함께 바뀌었다.

## 6. 이 과정에서 얻은 것

- **검토자를 분리한 것이 값어치를 했다.** 필수 3건 중 둘(`state.answered`, R3 모순)은 계획을 쓴 쪽이 두 번 읽고도 찾지 못한 것이었다. 둘 다 화면에는 아무 증상이 없고 검증까지 통과하는 종류다.
- **검토자도 틀린다.** 12건 중 2건(권고 1, 필수 3)은 검토 측 오류였고, 계획 측이 반박해 바로잡았다. 저장소 상태처럼 문서만으로 알 수 없는 것에서 어긋남이 났다.
- **"수용"과 "그대로 따름"은 다르다.** 필수 1, 2와 권고 8은 지적을 받아들이되 처방을 바꿨다. 문제 인식과 해결책은 따로 판단해야 한다.
- **합의한 항목이 일정에서 새는 일이 있다.** 권고 1이 판정만 되고 실행 목록에서 빠졌던 것을 검토 측이 다시 짚었다. 판정과 일정은 따로 세어 봐야 한다.
- **논쟁의 절반은 사실 확인이면 끝난다.** 권고 8은 세 라운드를 돌았지만, 두 폴더에 같은 이름의 다른 파일이 있다는 사실 하나가 확인되자 정리됐다.

## 7. 다음

계획서는 반영을 마쳤고 1단계에 들어갈 수 있다. 구현 결과물 검토는 **1단계 직후**에 다시 요청하기로 했다. 40문제 데이터와 검사 함수가 이후 모든 단계의 바닥이고, 40문제 육안 검토는 작성자 혼자 하면 놓치기 쉬운 종류이기 때문이다.
