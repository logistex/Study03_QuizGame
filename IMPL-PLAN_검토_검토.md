<!-- 생성: 2026-08-13 22:23 KST -->

# 구현 계획 검토 의견서에 대한 회신 검토

대상: 구현 계획 세션의 `IMPL-PLAN_검토.md` 응답 (12건 중 10건 수용, 1건 조건부, 1건 반려)
작성: 검토 의견서 작성 측

## 0. 총평

**응답의 판정은 대체로 옳고, 두 건은 제 판단을 교정했다.** 12건 전부를 다시 검토한 결과, 판정을 되돌려야 할 항목은 없다.

다만 반려 건(권고 8)의 근거 절반이 사실과 다르다. 결론은 그대로 두어도 되지만, 그 사실 오인이 남으면 구현 도중 낡은 PRD 사본을 집을 위험이 있으므로 아래 3절에 따로 적는다.

| 구분 | 건수 | 항목 |
|---|---|---|
| 회신 측 판단이 맞음 (검토 측 오류) | 2 | 권고 1, 필수 3 |
| 회신 측이 진단을 더 정확히 좁힘 | 2 | 필수 1, 필수 2 |
| 반려 수용, 근거 일부 정정 필요 | 1 | 권고 8 |
| 그대로 수용 | 7 | 권고 2~7, 9 |

---

## 1. 검토 측이 틀린 것 2건

### 권고 1 — `saveRanking` 반환 계약

`{ index, list }` 반환을 선호한 제안이 잘못이었다. PRD 3.4절이 반환값을 **정수, `null`, `'unavailable'` 세 가지로 명시**했고, 3.3절이 "저장 함수의 반환값으로 판단한다"고 못박았다. 구현 편의로 PRD 계약을 조용히 바꾸는 셈이었다.

PRD 부록이 경계하라고 한 것이 정확히 이 종류다 — 새 항목을 넣으며 기존 절과 어긋나는지 보지 않는 것. 계획 측이 이를 잡아낸 것이 맞다.

`renderResult`가 `readRankings()`를 다시 부른다고 적는 (a)로 간다. 읽기 한 번이 더 들지만 PRD 계약을 건드리지 않는다.

### 필수 3 — `git init`

저장소가 이미 존재한다는 사실은 계획 문서만으로는 알 수 없었다. `git init`을 넣으면 틀린 지시가 되는 것이 맞다.

**"작업 트리가 깨끗한 상태에서 시작한다"가 정확한 전제이고, 등급을 권고로 내린 것도 타당하다.** `git diff`를 1단계 원상 복구 확인에 쓰는 부분만 반영하면 원래 지적의 실질은 살아남는다.

---

## 2. 회신 측이 진단을 더 정확히 좁힌 것 2건

이 둘은 지적이 반박된 것도, 그대로 수용된 것도 아니다. 문제는 검토 측이 찾았고 처방을 회신 측이 더 정확한 형태로 만들었다.

### 필수 2 — 627줄 항목의 성격

라벨을 무엇으로 바꿀지에서 갈렸다.

| | 627줄을 무엇의 검증으로 보는가 |
|---|---|
| 검토 측 제안 | PRD 3.4절 — 같은 이름 기록을 합치지 않음 |
| 회신 측 판정 | `renderRankingTable(container, list, highlightIndex)` 계약 이탈 |

**회신 측이 맞다.** 그 항목은 두 가지를 본다.

1. 같은 이름, 같은 점수 두 줄이 각각 남는가
2. 강조된 줄이 하나뿐이고 두 줄 중 위쪽인가

검토 측 라벨은 1번만 설명한다. 그런데 1번은 PRD 규칙을 확인하는 쪽에 가깝고, **실제로 결함을 잡는 것은 2번**이다. `highlightIndex`를 무시하고 이름 대조로 강조 클래스를 거는 구현이면 두 줄이 모두 강조된다. 회신 측 라벨은 그 결함을 정면으로 가리킨다.

같은 항목이 무엇을 잡는 검증인지가 달라졌고, 회신 측 쪽에 잡는 것이 있다.

### 필수 1 — `state.answered` 처리 방향

검토 의견서는 두 안을 나열하고 판단을 넘겼다("대입 추가, **또는** 필드 제거"). 회신 측이 제거를 고르고 근거를 붙였다 — 버튼을 다시 활성화하는 경로가 없으므로 `disabled` 하나로 충분하고, 상태 필드를 늘리는 것보다 단순하다.

**근거가 성립한다.** 확인한 내용은 다음과 같다.

- 비활성 버튼은 자식 요소를 눌러도 클릭 이벤트가 발생하지 않는다. 정답 표시로 `✓`를 자식 요소로 붙여도 마찬가지다
- `handleAnswer`가 동기 함수라, 빠른 연타의 두 번째 클릭은 이미 비활성이 적용된 뒤에 도착한다
- 이벤트 위임을 쓰더라도 비활성 버튼에서는 이벤트가 올라오지 않는다

가드가 정말로 불필요하므로 필드 제거가 맞다. 결정을 미룬 쪽은 검토 측이고 회신 측이 근거를 갖춰 닫았다.

---

## 3. 반려 건 — 결론은 수용, 근거 하나는 정정

### 반려 자체는 옳다

회신 측 폴더에서 `PRD.md`가 최신본이라면, 검토 의견서의 권고대로 `PRD-v3.md`로 고치면 구버전을 가리키게 된다. **따르면 틀린다는 판단이 맞고 반려를 받아들인다.**

### 다만 사실 하나가 다르다

> PRD-v3.md는 수정 전 판본이다

**검토 측 폴더(`~/Downloads/PRD 검토`)의 `PRD-v3.md`는 수정본이다.** 확인 결과는 다음과 같다.

```
"다음 두 시점에 해제한다"  → 1건
"다음 세 시점에 해제한다"  → 0건
수정 시각: 2026-08-12 22:32
```

`beforeunload` 해제 시점을 셋에서 둘로 줄인 그 수정이 반영된 파일이고, 계획 세션으로 전달된 것도 이 파일이다. 회신 측 폴더에 별도의 `PRD-v3.md`가 있고 그것이 전달 이전 사본이라면, **낡은 것은 그 사본뿐이다.**

### 그래서 진짜 문제는 이름이 아니라 낡은 사본이다

같은 문서가 두 폴더에 세 파일로 존재하고 그중 하나가 낡았다. 지금은 어느 것이 낡았는지 알지만, 구현 도중 PRD를 다시 펼칠 일이 반드시 생긴다. 그때 `PRD-v3.md`라는 이름이 최신을 암시하므로 **오히려 함정이 된다.** 파일명 규칙으로는 해결되지 않는다.

낡은 사본을 집으면 `beforeunload` 해제 지점이 셋으로 되돌아가고, 도달 불가 경로에 해제 호출을 심는 원래 문제가 그대로 재발한다.

### 정리 방법 두 가지

**구현 착수 전에 처리하기를 권한다.** 어느 쪽이든 반려 취지를 해치지 않는다.

**(가) 낡은 사본을 지운다** — 가장 확실하다. 계획 세션 폴더에 PRD 사본을 하나만 남기고, 그것이 `PRD.md`이면 계획서 5줄을 고칠 필요도 없다.

**(나) 기준 문서 표기를 판본 내용으로 고정한다** — 사본을 남겨야 한다면 이름 대신 내용으로 판정되게 한다.

```
기준 문서: 상식 퀴즈 게임 PRD — beforeunload 해제 두 시점 반영본
(이 저장소에서는 PRD.md)
```

폴더가 달라져도 어느 판본인지 판정된다.

낡은 사본을 남겨 두어야 할 이유가 있다면 파일명 앞에 `_old`를 붙이거나 하위 폴더로 내려 두는 것만으로도 오집을 크게 줄일 수 있다.

---

## 4. 줄 번호 인용에 대하여

검토 의견서가 인용한 번호(626, 627, 480 등)는 **전달받은 사본 기준으로 정확하다.** 그 사본은 2026-08-12 22:47자이고 `beforeunload` 수정이 이미 반영된 상태였다. 3단계 3.6절에 "데이터 검사 실패는 해제 대상이 아니다"가 들어 있는 것을 확인하고 검토했다.

22:47 이후에 계획서를 더 고쳤다면 어긋났을 수 있다. **항목 이름으로 찾으라는 조언 자체는 옳으니 그대로 따르면 된다.** 다만 인용이 처음부터 낡았던 것은 아니므로, 다른 항목의 구체적 지적까지 의심할 필요는 없다.

---

## 5. 착수 전 확인 사항

회신 측의 등급 재조정에 동의한다.

**착수 전 필수 2건**

1. `state.answered` 필드 제거 — 상태 표에서 삭제, `handleAnswer` 1번을 주석으로 대체
2. R3 라벨 교체 — 627줄 항목을 `renderRankingTable` 계약 이탈 검증으로, 626줄에 "실행 검증 없음 — 코드 확인 필수" 강조

**착수 전 함께 처리를 권하는 것 1건**

3. 낡은 `PRD-v3.md` 사본 정리 (3절)

**해당 단계 진입 전**

- 3단계 전: 권고 5(카테고리 객체 조회), 권고 6(대비 사전 확인)
- 4단계 전: 권고 3(콘솔 `saveRanking`으로 기록 쌓기)
- 나머지 권고 4건은 구현 중 반영해도 무방하다

검토가 놓친 항목을 회신 측이 따로 찾지 못했다는 점, 필수 1번과 2번을 다시 읽어도 같은 결론이라는 점을 확인했다. 이 회신을 반영하면 구현에 들어가도 된다.
