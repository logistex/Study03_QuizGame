<!-- 생성: 2026-08-10 20:42 KST -->

# 상식 퀴즈 게임 PRD

> 최초 작성본이다. 개정 전 판본을 보존한 것이며, 현재 문서는 [PRD.md](PRD.md)를 볼 것.

## 1. 개요

### 1.1 목적

4지선다 객관식 상식 퀴즈 게임을 만든다. 플레이어는 이름을 입력하고 카테고리를 골라 문제를 풀며, 답을 고르는 즉시 정답 여부와 해설을 확인한다. 한 판이 끝나면 최종 점수를 확인하고, 기록이 상위권에 들면 순위표에 이름이 남는다.

### 1.2 대상 사용자

- 로그인이나 회원 가입 없이 바로 즐기려는 개인 사용자
- 브라우저에서 파일을 열기만 하면 실행되는 환경을 원하는 사용자

### 1.3 기술 스택

| 항목 | 선택 |
|---|---|
| 언어 | Vanilla JavaScript (ES6+) |
| 마크업, 스타일 | HTML5, CSS3 |
| 데이터 저장 | `localStorage` |
| 문제 데이터 | `questions.js` (전역 상수 선언) |
| 외부 라이브러리 | 없음 |
| 실행 방식 | `index.html`을 브라우저에서 직접 열기 (`file://`) |

### 1.4 파일 구성

| 파일 | 역할 |
|---|---|
| `index.html` | 세 화면(시작, 퀴즈, 결과)의 마크업을 모두 담는다 |
| `style.css` | 전체 스타일과 반응형 레이아웃 |
| `questions.js` | 카테고리 정의와 40문제 데이터 |
| `script.js` | 게임 상태 관리, 화면 전환, 채점, 순위 저장 |

`index.html` 하단에서 `questions.js`를 먼저, `script.js`를 나중에 읽는다.

```html
<script src="questions.js"></script>
<script src="script.js"></script>
```

### 1.5 문제 데이터를 JSON이 아닌 JS 파일로 두는 이유

`file://` 프로토콜로 페이지를 열면 브라우저의 보안 정책 때문에 `fetch('questions.json')`이 차단된다. 별도 서버 없이 실행해야 하므로, 문제 데이터를 `questions.js` 안의 전역 상수로 선언하고 `<script>` 태그로 읽어 들인다. 이렇게 하면 로컬 서버나 빌드 도구가 필요하지 않다.

---

## 2. 게임 규칙

- 4지선다 객관식이다.
- 카테고리는 한국사, 과학, 지리, 예술과 문화 네 가지다.
- 카테고리마다 10문제씩, 모두 40문제를 보유한다.
- 플레이 모드는 두 가지다.
  - **카테고리별 도전**: 고른 카테고리의 10문제를 푼다. 만점은 10점이다.
  - **전체 도전**: 네 카테고리 40문제를 섞어서 푼다. 만점은 40점이다.
- 문제 순서는 판을 시작할 때마다 섞는다. 선택지 순서는 고정한다.
- 정답은 1점, 오답은 0점이다. 난이도는 배점에 영향을 주지 않는다.
- 답을 고르면 즉시 정답 여부와 해설을 보여 주고, 그 문제는 다시 고를 수 없다.
- 한 판이 끝나면 최종 점수와 정답률을 보여 주고, 상위권 기록이면 순위표에 남긴다.

---

## 3. 기능 요구사항

### 3.1 시작 화면

- 게임 제목을 표시한다.
- 플레이어 이름 입력란을 둔다.
  - 최대 12자까지 입력받는다.
  - 공백만 입력한 상태에서는 게임을 시작할 수 없고, 안내 문구를 보여 준다.
- 카테고리 카드 네 개를 표시한다. 각 카드에는 카테고리 이름과 문제 수(10문제)를 적는다.
- "전체 도전 (40문제)" 버튼을 별도로 둔다.
- "순위표 보기" 버튼을 두어 저장된 기록을 확인할 수 있게 한다.
- 카드나 전체 도전 버튼을 누르면 해당 모드로 퀴즈를 시작한다.

### 3.2 퀴즈 진행 화면

화면 상단에 다음을 표시한다.

- 진행 상황 (예: `3 / 10 문제`)
- 현재 점수 (예: `2점`)
- 현재 문제의 카테고리 이름과 난이도 배지 (쉬움, 보통, 어려움)

본문에는 문제 텍스트와 선택지 버튼 네 개를 둔다.

선택지를 누른 뒤의 동작은 다음과 같다.

- 네 버튼을 모두 비활성화해 재선택을 막는다.
- 정답 버튼을 초록색으로 강조한다.
- 오답을 골랐다면 고른 버튼을 빨간색으로 함께 강조한다.
- 피드백 문구를 표시한다. 정답이면 "정답입니다.", 오답이면 "오답입니다. 정답은 ○○입니다."
- 해설 영역을 펼쳐 해당 문제의 해설을 보여 준다.
- "다음 문제" 버튼을 노출한다. 마지막 문제에서는 "결과 보기"로 표시한다.

**자동으로 다음 문제로 넘어가지 않는다.** 해설을 읽을 시간이 필요하므로, 플레이어가 버튼을 눌러야 넘어간다.

### 3.3 결과 화면

- 최종 점수를 표시한다 (예: `8 / 10 정답`).
- 정답률을 백분율로 표시한다.
- 플레이어 이름과 플레이한 모드를 함께 표시한다.
- 이번 기록이 순위표에 진입했다면 "순위표 N위 진입" 문구를 보여 준다.
- 해당 모드의 순위표를 함께 표시하고, 이번 기록에 해당하는 줄을 강조한다.
- 버튼 두 개를 둔다.
  - "다시 도전": 같은 모드를 새로 섞어 다시 시작한다.
  - "처음으로": 시작 화면으로 돌아간다.

### 3.4 순위 기록

순위표는 두 종류를 관리한다.

| 순위표 | 대상 | 보관 수 | 만점 |
|---|---|---|---|
| 카테고리별 | 카테고리별 도전 결과 | 카테고리마다 상위 5개 | 10점 |
| 통합 | 전체 도전 결과 | 상위 10개 | 40점 |

만점이 다른 두 결과를 한 표에 섞으면 비교가 성립하지 않으므로, 모드에 따라 기록할 순위표를 나눈다.

- 정렬 기준은 점수 내림차순이다. 점수가 같으면 먼저 달성한 기록을 위에 둔다.
- 보관 수를 넘어서면 가장 낮은 기록을 지운다.
- 저장 항목은 플레이어 이름, 점수, 총 문제 수, 달성 날짜다.

`localStorage` 저장 구조는 다음과 같다. 키는 `quizGameRankings` 하나를 쓴다.

```json
{
  "korean_history": [
    { "name": "홍길동", "score": 9, "total": 10, "date": "2026-08-10" }
  ],
  "science": [],
  "geography": [],
  "art_culture": [],
  "all": [
    { "name": "홍길동", "score": 34, "total": 40, "date": "2026-08-10" }
  ]
}
```

### 3.5 순위표 화면

- 시작 화면의 "순위표 보기" 버튼으로 진입한다.
- 카테고리별 순위표 네 개와 통합 순위표를 함께 보여 준다.
- 기록이 없는 순위표에는 "아직 기록이 없습니다."를 표시한다.
- "닫기" 버튼으로 시작 화면으로 돌아간다.

### 3.6 문제 데이터 (`questions.js`)

카테고리 정의와 문제 데이터를 각각 전역 상수로 선언한다.

```javascript
const CATEGORIES = [
  { id: 'korean_history', name: '한국사' },
  { id: 'science',        name: '과학' },
  { id: 'geography',      name: '지리' },
  { id: 'art_culture',    name: '예술과 문화' }
];

const QUESTIONS = {
  korean_history: [
    {
      id: 'kh_01',
      question: '조선을 건국한 인물은 누구인가?',
      options: ['이성계', '이방원', '정도전', '왕건'],
      answer: 0,
      difficulty: 'easy',
      explanation: '이성계는 1392년 고려를 무너뜨리고 조선을 세워 태조가 되었다.'
    }
    // ... 카테고리마다 10문제
  ],
  science: [],
  geography: [],
  art_culture: []
};
```

필드 규칙은 다음과 같다.

| 필드 | 설명 |
|---|---|
| `id` | 문제 식별자. 카테고리 약어와 두 자리 번호를 잇는다 (`kh_01`, `sc_07`) |
| `question` | 문제 텍스트 |
| `options` | 선택지 네 개의 배열 |
| `answer` | 정답의 인덱스 (0~3) |
| `difficulty` | `'easy'`, `'normal'`, `'hard'` 중 하나 |
| `explanation` | 해설. 한두 문장으로 정답의 근거를 설명한다 |

난이도 값을 영문 키로 두는 이유는 배지 색상을 CSS 클래스로 구분하기 위해서다. 화면에 보이는 "쉬움, 보통, 어려움" 라벨은 `script.js`에서 매핑한다.

카테고리마다 난이도를 고루 섞는다. 쉬움 3~4문제, 보통 3~4문제, 어려움 2~3문제를 기준으로 한다.

---

## 4. 비기능 요구사항

| 항목 | 내용 |
|---|---|
| 실행 | 별도 서버나 빌드 없이 `index.html`을 열어 동작한다 |
| 외부 의존성 | 없다. 웹 폰트, CDN, 라이브러리를 쓰지 않는다 |
| 반응형 | 모바일 세로 화면과 데스크톱을 모두 지원한다. 선택지 버튼은 터치하기 충분한 크기로 만든다 |
| 브라우저 | 최신 Chrome, Edge, Safari를 기준으로 한다 |
| 접근성 | 선택지 버튼에 키보드 초점이 보이도록 한다 |

### 4.1 에러 처리

실제로 일어날 수 있는 두 가지만 처리한다.

1. **`questions.js` 로드 실패**: `QUESTIONS`가 정의되지 않았으면 시작 화면에 "문제 데이터를 불러오지 못했습니다."를 표시하고 게임 시작을 막는다.
2. **`localStorage` 사용 불가**: 사파리 비공개 모드 등에서 저장이 막힐 수 있다. 읽기와 쓰기를 `try/catch`로 감싸고, 실패하면 순위 저장만 건너뛴 채 게임은 정상 진행한다.

---

## 5. 화면 흐름

```
[시작 화면]
  이름 입력 → 카테고리 카드 선택 또는 전체 도전
        │                                    └─→ [순위표 화면] ─→ 시작 화면
        ▼
[퀴즈 진행 화면]
  문제 표시 → 선택지 클릭 → 즉시 피드백 + 해설 → "다음 문제"
        │                                              │
        └──────────── 남은 문제 있음 ←─────────────────┘
        │ 마지막 문제 완료
        ▼
[결과 화면]
  점수, 정답률, 순위표 진입 여부
        ├─ "다시 도전" → 퀴즈 진행 화면 (같은 모드, 새로 섞음)
        └─ "처음으로"  → 시작 화면
```

---

## 6. 구현 단계

| 단계 | 내용 | 완료 기준 |
|---|---|---|
| 1 | `questions.js` 작성 | 네 카테고리에 10문제씩, 모든 문제에 난이도와 해설이 채워져 있다 |
| 2 | `index.html` 골격과 시작 화면 | 이름 입력, 카테고리 카드, 전체 도전 버튼이 보이고 화면 전환이 동작한다 |
| 3 | 퀴즈 진행과 즉시 피드백 | 문제가 순서대로 나오고, 정답, 오답 강조와 해설이 표시되며 점수가 누적된다 |
| 4 | 결과 화면과 순위 기록 | 최종 점수가 나오고 `localStorage`에 순위가 저장되며, 다시 열어도 기록이 남아 있다 |
| 5 | `style.css` 마무리와 에러 처리 | 모바일 폭에서 레이아웃이 깨지지 않고, 위 4.1의 두 상황이 처리된다 |

---

## 7. 범위에서 제외한 것

다음은 이번 구현에 넣지 않는다.

- 제한 시간(타이머)과 시간 기반 점수 가중치
- 난이도별 배점 차등, 난이도 필터 선택
- 오답 노트, 틀린 문제만 다시 풀기
- 마지막 플레이어 이름 자동 입력
- 사운드 효과, 화면 전환 애니메이션
- 서버 연동, 온라인 순위표, 문제 데이터 원격 갱신
