<!-- 생성: 2026-08-13 23:25 KST -->

# 1단계 검토 요청

- **요청 근거**: [IMPL-PLAN_검토_검토_회신.md](IMPL-PLAN_검토_검토_회신.md) 6절. 구현 결과물 검토를 1단계 직후에 요청하기로 합의했다.
- **대상 커밋**: `0a6097e`
- **작업 내용**: [1단계_작업보고.md](1단계_작업보고.md)

## 이 검토가 필요한 이유

계획서 1.8절의 육안 검토를 **문제를 쓴 쪽이 그대로 수행했다.** `answer`가 0~3 안에 있으면서 틀린 선택지를 가리키는 경우와 해설이 정답과 무관한 경우는 검사 함수도 화면도 잡지 못하는데, 작성자가 검토하면 자기 판단을 되풀이하게 된다.

**작성 측이 검토를 마쳤다는 사실을 신뢰하지 말고, 원본 지식으로 다시 대조해 주기를 요청한다.**

## 검토 대상

### 1. `questions.js` 40문제 — 최우선

문항마다 다음을 봐 주기 바란다.

- `options[answer]`가 실제로 그 문제의 정답인가
- `explanation`이 그 정답의 근거를 설명하는가. 문제를 되풀이하는 문장에 그치지 않는가
- 해설에 담긴 연도, 수치, 인명이 사실인가
- 오답 세 개가 명백한 오답인가. **정답이 둘로 읽히는 문제가 없는가**

마지막 항목이 특히 위험하다. 예를 들어 `ac_05`는 「씨름」과 「서당」을 지목해 김홍도로 좁혔지만, 작품명을 빼면 신윤복도 답이 될 수 있는 문제였다. 이런 식으로 좁히기가 부족한 문항이 남아 있는지 봐 주기 바란다.

### 2. `script.js`의 `validateData()`

- PRD 4.2절 2번의 아홉 항목을 실제로 다 덮는가
- 통과는 시키되 느슨하게 구현된 검사가 있는가
- 콘솔 출력이 데이터를 쓰는 사람에게 충분한가

## 판단을 요청하는 것 (계획 이탈 2건)

| # | 내용 | 물음 |
|---|---|---|
| 1 | 1.5절 검사 함수 검증을 파일 수정 대신 브라우저 메모리에서 수행 | 절차 이탈을 받아들일 만한가. 계획서 1.5절을 이 방식으로 고쳐야 하는가 |
| 2 | 로컬 확인용 `.claude/launch.json` 추가, `.gitignore`에 `.claude/` 추가 | 계획에 없던 파일이다. 남겨도 되는가 |

## 범위 밖

다시 논의하지 않는다.

- 2단계 이후의 설계와 일정
- PRD 확정 사항 (한 판 10문제, 3+3+2+2 배분, 순위표 5개 독립, GitHub Pages 배포)
- PRD 4.1절 알려진 제약 4건
- 이미 합의를 마친 계획 검토 12건 ([구현_계획_검토_요약.md](구현_계획_검토_요약.md))

## 재현 방법

저장소를 받은 뒤 폴더에서 로컬 서버를 띄우고 브라우저로 연다.

```bash
python3 -m http.server 8000
```

콘솔에서 다음을 확인할 수 있다.

```javascript
validateData()                                   // [] 이어야 한다
Object.values(QUESTIONS).map(a => a.length)      // [10, 10, 10, 10]
Object.values(QUESTIONS).flat().map(q => q.id + ' -> ' + q.options[q.answer])
```

## 회신에 담아 주면 좋을 것

- 사실이 틀린 문항의 `id`와 무엇이 틀렸는지
- 정답이 둘로 읽히는 문항의 `id`와 어느 선택지가 걸리는지
- 검사 함수에서 빠졌거나 느슨한 항목
- 계획 이탈 2건에 대한 판단

문항 교체가 필요하면 대안까지 주지 않아도 된다. 어느 문항이 왜 걸리는지만 알려 주면 이쪽에서 고쳐 다시 검토를 요청한다.
