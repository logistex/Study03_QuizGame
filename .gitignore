# 교재 집필용 내부 문서 (공개하지 않음)
doc/

# macOS
.DS_Store
PRD 검토 가상본

# 로컬 미리보기 서버 설정 (배포 산출물 아님)
.claude/*

# 커스텀 명령어와 스킬은 남긴다. 수정 이력이 문서로 기록되어야 한다
!.claude/commands/
!.claude/skills/

# 화면 캡처 원본 — 계정 정보와 키가 그대로 보임
capture/raw/
