<!-- 생성: 2026-08-16 11:38 KST -->

# 이 저장소에서 일하는 방법

『바이브 코딩』 5장 실습이다. **PRD를 쓰고, 새 세션에서 검토받아 고치고, 계획을 세우고, 다시 검토받고, 구현하고, 또 검토받는 과정 자체가 결과물이다.** 그래서 오간 문서를 지우지 않고 `history/`에 번호순으로 남긴다. 틀린 판단도 정정 문단을 달아 남긴다.

## 무엇을 먼저 읽나

| 무엇이 궁금한가 | 어디를 보나 |
|---|---|
| 무엇을 만드는가 | `spec/PRD.md` |
| 어떻게 만들고 확인하는가 | `spec/IMPL-PLAN.md` |
| 지금까지 오간 것 | `history/01`~, 번호순이 시간순 |
| 지금 열려 있는 것 | `history/25-open-items.md` |

## 단계마다 하는 일

1. 챕터 표시를 남긴다. **일이 실제로 시작될 때** 찍는다. 미리 찍으면 엉뚱한 작업이 그 아래 들어간다
2. 계획서의 해당 단계 절을 읽고 그대로 구현한다
3. 완료 기준과 체크리스트를 하나씩 밟는다
4. 커밋하고 태그를 붙인다 — `git tag stage4 && git push origin --tags`
5. 작업 보고서를 `history/`에 쓴다
6. 노트용 요약을 만들어 노션에 넣는다
7. README의 단계 표와 문서 목록을 갱신한다

검토 지적을 반영할 때는 하나가 더 붙는다.

8. **바꾼 동작을 서술한 자리를 전부 맞추고 그 사실을 알린다.** 코드, 계획서 본문, 함수 계약, PRD, **뒤 단계의 검증 절차와 체크리스트**가 모두 그 자리다. 회신을 쓴 뒤에 결정이 더 나오면 회신 문서에 덧붙임을 남긴다

**"셋"으로 세지 않는다.** 처음에는 "코드, 계획서, PRD 셋을 맞춘다"로 적었는데, 그 "셋"이 범위를 세 파일로 좁혔다. 세 파일은 맞췄는데 5단계 검증 절차를 빠뜨렸고, 그대로 밟았으면 정상인 것을 결함으로 판정했을 뻔했다. 규칙을 둘로 나눠 두면 "앞엣것 했으니 됐다"는 판단이 다시 생긴다.

**찾는 방법은 둘을 지킨다.**

- **문구가 아니라 식별자로 찾는다.** 요소 id와 함수 이름은 동작이 바뀌어도 그대로다. 화면 문구로 찾으면 문구가 바뀌는 변경에서 놓친다
- **고치기 전에 검색해 목록을 만든다.** 고친 뒤에 검색하면 이미 바꾼 자리가 안 걸려 목록 자체가 불완전해진다

```bash
grep -rn "btn-clear\|renderRankingScreen" spec/   # 고치기 전에, 식별자로
```

목록을 만들어 두면 다 밟았는지가 판단이 아니라 대조가 된다.

## 검토 지적을 받으면

**먼저 재현해 사실을 확인한 뒤 고친다.** 1차 개발 1단계에서 이 방식으로 검토 측 오류 두 건을 잡았고, 4단계에서는 지적이 사실임을 재현으로 확인한 뒤 고쳤다.

- 지적을 그대로 받지 않고 **처방을 바꾸는 것은 된다.** 다만 왜 바꿨는지를 회신에 적는다
- 근거가 틀렸다는 지적을 받으면 결론이 같더라도 **근거를 고친다.** 틀린 전제가 남으면 나중에 그 전제로 다른 결정을 내릴 때 틀린다

## 규격이란 무엇인가

**`spec/` 폴더의 두 문서를 함께 말한다.** PRD만 뒤지고 "규격에 없다"고 하지 않는다.

- `spec/PRD.md` — 무엇을 만드는가. 정본이다
- `spec/IMPL-PLAN.md` — 어떻게 만들고 확인하는가. 육안 검토 절차가 여기 있다

`history/`의 검토 문서는 규격이 아니다. 관점의 출처일 뿐이고, 채택된 권고는 `spec/`으로 옮겨져 있다.

이 구분을 안 지켜 사고가 났다. 서로 못 보고 만든 두 판이 같은 자리에서 틀렸다 — 출처 둘 이상 대조를 "규격 밖"으로 표시했는데 `spec/IMPL-PLAN.md` 1.8절 7번에 있다.

**문서나 규격의 낱말을 그대로 옮길 때는 그 분류가 지금도 맞는지 확인한다.** 확인하지 않았으면 "확인하지 않았다"를 함께 적는다.

낱말은 쓰인 시점의 도구를 담고 있다. 그대로 옮기면 그 시점에 갇힌다.

- 계획서가 "육안 검토"라고 적은 것을 "사람이 보는 항목"으로 옮겼는데, 그 여덟을 지금은 클로드가 판정한다. 검사 함수가 못 잡는다는 뜻이지 모델이 못 한다는 뜻이 아니었다
- "규격이 정한 실행 경로는 브라우저"라고 옮기고 명령줄 경로가 없는 것을 규격 밖이라 했는데, **없다는 것 자체가 규격의 공백**이었다

## 규격 동결

**1차 개발 2단계부터 6단계까지 PRD를 고치지 않는다.** 규격 변경이 필요하면 PRD 8절(2차 구현 예정)로 보낸다.

예외는 하나다. **코드와 규격이 어긋난 것을 발견했을 때.** 새 요구를 넣는 것이 아니라 이미 구현된 것을 적는 일이라 설계를 흔들지 않는다.

계획서도 같은 기준이다. 남은 단계의 함수 계약과 요소 id 계약은 건드리지 않는다. 다만 **이미 끝난 단계의 검증 절차를 고치는 것은 된다.** 계획서는 "어떻게 확인하는가"라 산출물이 바뀌지 않기 때문이다.

## 검증

- **1차의 검증 수단은 셋이다** — 데이터 검사 함수, 클로드의 판정, 그리고 브라우저에서 직접 확인하는 수동 체크리스트. 테스트 프레임워크를 도입하지 않는다
- **2차에서 넷째가 붙는다 — 거절 확인이다.** 서버가 막아야 할 것을 실제로 막는지 본다. 단계마다 권한 경계가 새로 생기고, **막히는지는 화면이 정상으로 보이는 것과 무관하다**(`spec/IMPL-PLAN-2.md` "검증 셋")
- **1차는 수동 체크리스트를 개발 5단계에 몰았으나 2차는 그럴 수 없다.** 단계마다 셋을 밟고 마지막 단계 뒤에 둘을 더 밟는다
- **1차와 2차에서 데이터 검사가 도는 자리가 다르다.** 1차는 페이지 로드 시점의 브라우저이고 2차는 문항을 넣는 시점의 서버다(`spec/PRD.md` 1.5절)
- **클로드가 판정하고 사람이 채택 여부를 결정한다.** 문항 규칙 여덟 가지가 이 자리다(`spec/PRD.md` 3.6절, `spec/IMPL-PLAN.md` 1.8절). 검사 함수와 달리 재현되지 않으므로 **서로 다른 세션 둘이 각각 한 번씩** 돌고 라운드마다 기록을 남긴다
- **앱 안에서는 그 판정을 한 번만 돌린다.** 앱에 세션이 없어 두 번째 라운드를 선생님의 채택이 대신한다. **규격을 한 칸 완화한 것이며**, 문제은행을 크게 늘릴 때는 세션 둘이 도는 경로를 그대로 쓴다(`spec/PRD.md` 3.9절)
- **콘솔 대체를 허용하는 자리에는 그 대체가 건너뛰는 경로를 함께 적는다.** 무엇을 대신할 수 있는지만 적으면, 대신하는 순간 무엇이 확인되지 않는지가 남지 않는다
- **화면에 증상이 없는 결함이 이 프로젝트의 주된 위험이다.** 최종 상태만 보는 확인은 경로에서 생기는 결함을 놓친다. 초점 이동은 `focusin` 기록으로, 순위 인덱스는 코드 확인으로 판정한다

### 도구의 한계

- **브라우저 자동화가 키 이벤트의 기본 동작을 일으키지 못한다.** `keydown:Enter`는 도착하는데 클릭이 합성되지 않는다. 초점 이동까지만 자동으로 확인하고 **누르는 것은 사람에게 맡긴다**
- `confirm` 확인창도 자동으로 띄울 수 없다. 핸들러가 붙어 있는지를 세어 판정한다
- **스크린리더는 사람만 확인할 수 있다.** 소리를 잡을 수 없다

## 개발 환경

`.claude/`에 `serve.py`와 `launch.json`이 있다. **둘 다 gitignore 대상이라 새 세션에는 없다.** 없으면 아래를 다시 만든다.

```python
# .claude/serve.py
import http.server


class NoCacheHandler(http.server.SimpleHTTPRequestHandler):
    def end_headers(self):
        self.send_header('Cache-Control', 'no-store')
        super().end_headers()


if __name__ == '__main__':
    http.server.test(HandlerClass=NoCacheHandler, port=8010, bind='127.0.0.1')
```

**브라우저가 `script.js`를 캐시해 새 코드가 실행되지 않는 일이 1차 개발 3단계까지 매번 생겼다.** 원인은 `python3 -m http.server`가 `Cache-Control`을 보내지 않고 `Last-Modified`만 보내는 것이다. 강제 새로고침과 새 탭으로는 안 풀렸고 포트를 바꾸는 것만 통했는데, 새 오리진에 캐시 항목이 없어서였다.

- **포트를 바꿔 가며 개발하지 않는다.** 오리진이 달라져 `localStorage`가 분리되고, 1차 개발 4단계부터는 순위 기록이 사라진 것처럼 보인다
- 포트는 **8010**이다. PRD 4.1절은 8000으로 적었으나 다른 작업이 그 포트를 쓰고 있어 비켜 두었다. 중요한 것은 값이 아니라 고정이다
- 로컬에서 `file://`로 열면 사파리는 `localStorage` 접근에서 오류를 낸다. 로컬 확인은 크롬이나 엣지로 한다(PRD 4.1절)

## 배포

`gh-pages` 브랜치가 배포물이다. `main`은 소스와 문서만 담는다.

```
gh-pages/
├── index.html, style.css, script.js, questions.js   최신본
├── .nojekyll
├── stage1-final/   각 단계의 검토 반영까지 끝난 시점, 파일 넷만
├── stage2/         2단계만 -final이 없다
├── stage3-final/
├── stage4-final/
└── stage5-final/
```

태그가 바뀌면 그 폴더만 다시 만든다. `git archive`가 태그 시점의 파일을 그대로 꺼내므로 워크트리를 만들 필요가 없다.

**뿌리의 최후본도 `main`이 아니라 `stage6-final`에서 꺼낸다.** 2차 구현의 소스 코드가 `main`에 올라가는데, 2차 앱은 서버 함수가 있어야 동작하므로 `gh-pages`에 올리면 깨진다. `main`에서 꺼내면 1차 배포 자리에 동작하지 않는 앱이 올라간다.

```bash
git worktree add --detach /tmp/ghp && cd /tmp/ghp && git checkout gh-pages
git archive stage6-final index.html style.css script.js questions.js | tar -x
for t in stage1-final stage2 stage3-final stage4-final stage5-final; do
  mkdir -p "$t" && git archive "$t" index.html style.css script.js questions.js | tar -x -C "$t"
done
```

- **문서는 복사하지 않는다.** `spec/`, `history/`, `archive/`까지 넣으면 같은 문서가 여섯 벌 생긴다
- **여섯 주소가 같은 오리진이라 `localStorage`를 공유한다.** 1차 개발 3단계를 보다 4단계로 가면 앞서 쌓인 기록이 그대로 보인다. 저장 키에 단계를 붙이는 것은 배포본 코드를 고치는 일이라 하지 않고, 노트에 한 줄 적는 것으로 갈음한다

## 2차 구현

1차 구현(1~6단계)은 배포로 닫혔다. 2차 구현은 문서를 폴더로 나눈다.

```
spec/               PRD.md — 정본 하나. 2차에서도 이것을 고친다
spec/IMPL-PLAN-2.md 2차 구현 계획
history/impl_v2/    2차에서 오간 문서. 번호는 1차에서 이어 붙인다
archive/            개정 전 판본. 1차와 나누지 않는다
.claude/commands/   커스텀 명령어. 수정 이력이 남도록 저장소에 둔다
```

- **PRD는 나누지 않는다.** 정본이 둘이 되면 어느 쪽이 맞는지 매번 판단하게 된다. 2차 착수 시점의 PRD를 `archive/`에 보존하고 `spec/PRD.md`를 고친다
- **계획서는 나눈다.** 1차 계획서가 900줄에 가깝고, 2차는 단계와 검증이 다르다
- **문서 번호는 이어 붙인다.** 폴더가 갈려도 "17번 문서"가 하나를 가리켜야 한다

**1차 문서는 옮기지 않는다.** `history/` 뿌리에 그대로 두고 2차만 `history/impl_v2/`에 담는다. 폴더가 비대칭이 되지만 링크가 하나도 안 깨진다.

옮기면 `history/`를 가리키는 자리 약 160곳이 걸리는데, 그 절반 이상이 `archive/`와 `consensus/`에 있다. **둘 다 과거 기록이라 고치면 그 시점의 기록이 아니게 된다.** 노션 노트가 `blob/main/history/`로 거는 링크도 함께 깨지고, 노션은 편집 한도에 걸려 손보기 어렵다.

## `CLAUDE.md`를 고칠 때

**고치기 전 판을 `archive/CLAUDE-vN.md`로 남긴다.** 첫머리에 어느 시점인지와 현재 문서가 어디인지를 적는다. 노션 노트가 그 판본을 링크로 참조한다.

| 판본 | 시점 |
|---|---|
| `CLAUDE-v1.md` | 최초본. 규칙을 인계 문서에서 옮겨 온 시점 |
| `CLAUDE-v2.md` | 1차 구현을 마치고 5, 6단계 검토까지 반영한 시점 |
| `CLAUDE-v3.md` | 세 세션 합의로 명령어 생성 프롬프트를 정하기 직전 시점 |
| `CLAUDE-v4.md` | 확인 수단을 "클로드가 판정"으로 바꾸기 직전 시점 |
| `CLAUDE-v5.md` | 판정 두 번을 서로 다른 세션이 하도록 조이기 직전 시점 |
| `CLAUDE-v6.md` | 대조 단위와 세션 식별을 규격에 넣기 직전 시점 |
| `CLAUDE-v7.md` | 2차 문서 폴더를 `history/impl_v2/`로 정하기 직전 시점 |
| `CLAUDE-v8.md` | `gh-pages` 재생성 명령이 `main`을 참조하던 시점. 2차 소스를 `main`에 두기로 정하기 직전 |
| `CLAUDE-v9.md` | 태그 표가 실제 태그와 어긋나 있던 시점 |
| `CLAUDE-v10.md` | 지시 정확성 규칙 다섯을 넣기 직전 시점 |

### 어느 파일을 얼마나 자주 남기나

**두 파일의 성격이 다르다.**

| 파일 | git이 보존하나 | 사본을 언제 남기나 |
|---|---|---|
| 저장소 `CLAUDE.md` | 예. 모든 판이 커밋에 있다 | **결정 시점마다.** 노션 노트가 링크로 참조할 앵커가 그 구실이다 |
| `~/.claude/CLAUDE.md`, `~/.claude/STYLE.md` | **아니오.** 저장소 밖이다 | **고칠 때마다.** 사본이 유일한 기록이다 |

앞엣것은 문장 손질까지 판본으로 남기면 표가 의미를 잃는다. 뒤엣것은 사본을 거르면 그 판이 영영 사라진다.

**저장소 밖 설정 파일의 사본은 여기 둔다.**

| 사본 | 원본 | 어느 시점 |
|---|---|---|
| `CLAUDE-global.md` ~ `-v5.md` | `~/.claude/CLAUDE.md` | v3은 지시 정확성 규칙 직전, v4는 문체 규정 직전, **v5가 현재 판** |
| `STYLE-v1.md`, `-v2.md` | `~/.claude/STYLE.md` | v1은 규칙 다섯 직전, **v2가 현재 판** |

**뒤엣것은 「고치기 전 판」이 아니라 「현재 판」을 남긴다.** 저장소 밖 파일은 고친 뒤에야 사본을 뜰 수 있는 회차가 생기므로, 현재 판을 두고 다음 수정 때 그것을 직전 판으로 삼는다.

## 저장소 구조와 태그

```
spec/      PRD.md, IMPL-PLAN.md — 현행 규격
history/   오간 문서, 번호순이 시간순. 1차는 뿌리에, 2차는 impl_v2/에
consensus/ 세 세션이 함께 결론을 내는 문서, 라운드순. 규칙은 consensus/00-rules.md
archive/   개정 전 판본 (PRD-v1~v3, README-v1~v3)
doc/       교재 집필용 내부 문서 (gitignore)
```

태그는 **그 단계 작업이 끝난 시점**을 가리킨다. 노트의 산출물 표가 이 태그로 소스를 링크한다.

| 태그 | 커밋 | 뜻 |
|---|---|---|
| `stage1` | `0a6097e` | 1차 개발 1단계 구현을 마친 시점 |
| `stage1-final` | `3bfdfb0` | 1차 개발 1단계 검토 반영까지 |
| `stage2` | `2001225` | 1차 개발 2단계 구현을 마친 시점 |
| `stage3` | `741801f` | 1차 개발 3단계 구현을 마친 시점 |
| `stage3-final` | `17b169a` | 1차 개발 3단계 검토 반영까지 |
| `stage4` | `6c0b644` | 1차 개발 4단계 구현을 마친 시점 |
| `stage4-final` | `7391a75` | 1차 개발 4단계 검토 반영까지 |
| `stage5` | `48fbc4b` | 1차 개발 5단계를 마친 시점 |
| `stage5-final` | `48fbc4b` | `stage5`와 같은 커밋 |
| `stage6` | `93e68d9` | 1차 개발 6단계를 마친 시점 |
| `stage6-final` | `93e68d9` | `stage6`과 같은 커밋. **1차 구현의 끝** |
| `phase2-start` | `67737a7` | 2차 구현 착수 직전. 1차 코드에 문항만 60개 |

**1차 개발 2단계에는 `-final`이 없다.** 그 단계의 검토를 3단계와 묶어 받아 단독 최종본이 존재하지 않는다. 노트의 단계별 실행 링크는 `-final`이 있으면 그것을, 없으면 원래 태그를 쓴다.

**1차 개발 5단계와 6단계는 `-final`이 원래 태그와 같은 커밋을 가리킨다.** 검토 반영에 코드 변경이 없었기 때문이다. 5단계는 점검만 한 개발 단계였다.

**`phase2-start`는 개발 단계가 아니다.** 1차 앱 코드 그대로이고 문제은행만 60문항으로 확대된 시점이며, 다음 커밋부터 2차 문서 작업이 시작된다. 문제은행 확대를 PRD 8.1절이 2차 구현으로 분류하므로 **60문항은 1차의 결과가 아니라 2차의 첫 산출물이다.**

- 배포본 여섯은 40문항이라 이 태그와 다르다
- 노트가 1차 앱의 **소스**를 가리킬 때 이 태그를 앵커로 쓴다. **화면**을 가리킬 때는 `gh-pages` 주소를 쓴다

## 커스텀 명령어와 스킬을 만들 때

**만들 때마다 프롬프트에 다시 적지 않아도 되도록 여기 둔다.**

- **앞머리는 YAML이다.** `argument-hint` 값이 대괄호로 시작하면 따옴표로 묶는다. 안 묶으면 목록으로 읽혀 깨지고, 클로드 코드는 그대로 실행하므로 깃허브 화면에서만 드러난다
- **만들면 그 자리에서 한 번 돌려 확인한다.** 만들어 놓고 끝내지 않는다. 돌려 보지 않은 절차는 그럴듯한 문장으로 남는다
- 새 파일 첫머리의 생성 일시는 아래 문서 작성 규칙을 따른다

**여기 두지 않는 것이 있다.** "대상 파일을 고치지 말라"는 명령어마다 다르다. `quiz-validate`는 고치면 안 되고 `quiz-add`는 고치는 것이 일이다. 명령어의 성격에서 나오는 것은 그 명령어의 프롬프트에 적는다.

## 문서 작성

**문체는 음슴체와 문어체다.** `~/.claude/CLAUDE.md` 6.2절이 정본이며 구어체, 문어체 대응표가 거기 있다.

- **앞으로 새로 만드는 문서를 음슴체로 쓴다.** 규격, README, 보고서가 모두 대상이다
- **기존 문서의 일부를 고칠 때는 그 문서의 문체를 따른다.** 한 문서 안에서 종결어미를 섞지 않는다
- **볼드는 규격 문서에서 허용하되 절제한다.** 강의 노트에서는 쓰지 않는다
- **DB나 표의 행에는 「행」을 쓴다.** 문서의 줄에는 「줄」을 그대로 쓴다. 이 저장소에서 서른다섯 곳을 고쳤다

이 저장소의 현행 문서 넷이 아직 서술체다. 전환하려면 서술형 종결 약 620곳과 구어체 용어 약 250곳을 고쳐야 하므로, 문서 전체를 다시 쓰는 회차에 맞춰 한 문서씩 옮긴다.

| 문서 | 서술형 종결 | 구어체 용어 |
|---|---|---|
| `spec/PRD.md` | 294 | 61 |
| `spec/IMPL-PLAN-2.md` | 184 | 99 |
| `CLAUDE.md` | 102 | 60 |
| `README.md` | 45 | 34 |

**지시가 무엇을 가리키는지 밝히는 규칙 다섯은 `~/.claude/CLAUDE.md` 6.1절에 있다.** 저장소 문서와 강의 노트와 대화에 모두 적용한다. 여기 옮겨 적지 않는다.

이 저장소에서 그 다섯이 걸린 자리 둘.

- **"4.2절과 6절을 확인한다"**(`history/impl_v2/63-round2-adoption.md` 3절) — 문서 이름이 없어 PRD의 절인지 계획서의 절인지 문장으로 갈리지 않았다. 앞줄이 `spec/IMPL-PLAN-2.md`라 오히려 계획서 쪽으로 읽혔다
- **"두 자리를 확인하는 것"** — 세는 단위가 "자리"였고, 같은 동작을 앞줄은 "지난다"로 뒷줄은 "확인하는 것"으로 불렀다

고치기 전에 아래를 돌린다. 셋째부터 다섯째 규칙은 검색에 걸리지 않으므로 클로드가 판정한다.

```bash
grep -rnE '(둘|셋|넷|다섯|여섯|일곱|여덟|아홉|열|열한|열두|열세|스물)[ ]?(자리|곳)' spec/
grep -nE '[0-9]+\.[0-9]+절' spec/IMPL-PLAN-2.md \
  | awk -F: '{n=$1; l=$0; sub(/^[0-9]+:/,"",l);
              if (l !~ /(PRD|계획서|노트|[0-9]+번) ?[0-9]+\.[0-9]+절/) print n": "l}'
```

**둘째 줄을 `spec/PRD.md`에는 돌리지 않는다.** PRD는 자기 절을 이름 없이 가리키는 것이 맞고, 그 참조가 여든 줄이 넘어 목록이 좁혀지지 않는다. `spec/IMPL-PLAN-2.md`는 소수점 절 번호를 두지 않았으므로 거기 나오는 소수점 절은 모두 다른 문서의 것이다.

**`grep -r`의 출력을 그대로 `grep -v`에 넘기지 않는다.** 줄 앞에 붙은 파일 이름이 필터에 걸려 `spec/PRD.md`가 통째로 사라진다. 결과가 비어도 깨끗한 것이 아니라 검사가 안 돈 것이다. 이 덫에 두 번 걸렸다.

**과거 실행 기록에는 소급하지 않는다.** `history/`와 `archive/`는 그 시점의 기록이다. `spec/`, `.claude/commands/`, README, 앞으로 쓰는 문서에만 적용한다.

### 장과 절을 가른다

`##`이 장이고 `###`이 절이다. **`spec/`과 `.claude/commands/`에서 서른여덟 자리를 절에서 장으로 고쳤다.**

| 무엇 | 어떻게 | 보기 |
|---|---|---|
| 현행 규격의 `##` 층위 | 장 | PRD 6장, PRD 4장 외부 의존성 |
| 현행 규격의 `###` 층위 | 절 그대로 | PRD 4.2절, PRD 3.10절 |
| `history/`와 `archive/`를 가리키는 것 | **절 그대로** | 65번 9절, `archive/PRD-v5.md` 6절 |

**셋째 줄이 예외처럼 보이나 예외가 아니다.** 65번은 자기 `## 9.`를 본문에서 "9절"이라 부르고 그 문서를 고칠 수 없다. 층위대로 "65번 9장"이라 적으면 65번 안에서 그 낱말을 찾을 수 없다.

**`spec/IMPL-PLAN.md`는 한 자리만 고쳤다.** 이 문서가 "PRD"라고 부르는 것은 대부분 `archive/PRD-v5.md`이고 그 사실을 문서 앞머리가 선언해 두었다. 현행 `spec/PRD.md`를 가리킨 한 자리만 장으로 바꿨다.

- 요약문은 **개조식**으로 쓴다. 마침표를 붙이지 않고 굵게 표기도 쓰지 않는다. 20~30줄로 짧게
- 설명이 필요한 낱말을 쓰지 않는다. "탐침", "산문" 같은 표현으로 두 번 지적받았다
- 가운뎃점 대신 쉼표를 쓴다
- 새로 만드는 파일 첫머리에 `<!-- 생성: YYYY-MM-DD HH:MM KST -->`를 넣는다. 시각은 `TZ=Asia/Seoul date`로 확인한다
- **앞머리에 적은 세션 이름으로 누가 썼는지 판정하지 않는다.** 남의 이름을 적어도 걸러지지 않는다. 서로 다른 세션인지는 `git log`의 커밋 신원과 해시로 가른다
- **번호를 붙일 때는 마지막 번호를 확인하고 다음 번호를 쓴다.** 경로가 비었는지만 보고 번호가 비었는지를 안 봐서 17번이 겹친 적이 있다

## 노션 노트

- 페이지: `5장. 체계적인 개발과 관리 (퀴즈 게임 앱) C`
- 페이지 id: `3c0022e2-cfe7-8060-a7e6-ddcf7fa080c9`
- 구조: `5.3.N`이 2차 구현의 각 목, 그 아래 `5.3.N.M`과 `a)`, `b)`, `c)`

**페이지가 A, B, C 셋이다.** 커지면 새로 판 것이고 C가 현행이다. A(`3b8022e2-…`)는 1차 구현을 담고 구조가 `5.1.6.N`이다. **A를 고치면 안 되는 자리를 고치게 된다.**

편집 규칙이 있다. 사고를 내고 얻은 것이다.

1. **넣기 전에 그 자리 원문을 탭까지 그대로 확인한다.** 노션 본문은 탭으로 들여쓰고 공백이 아니다
2. **붙일 자리의 앞뒤를 포함한 유일한 문자열**을 `old_str`로 잡는다
3. **넣은 뒤 이웃 블록이 그대로인지 확인한다.** 이것을 빠뜨려 블록 하나를 잃은 적이 있다
4. 한글을 유니코드 코드값으로 적다 오타를 여러 번 냈다. **넣은 뒤 반드시 되읽는다**
5. 페이지가 커지면 편집이 통째로 막힌다(`rich_text_element_count` 한도). 한 줄 수정도 거부되므로 페이지를 줄여야 한다
6. **`<br>`가 든 불릿을 건드리면 그 불릿이 통째로 다시 파싱된다.** `old_str`을 `<br>` 바깥으로 잡아도 `<br>`로 이어 놓은 줄들이 자식 불릿으로 갈린다. 글자는 그대로이나 계층이 바뀌므로, 그 자리를 고칠 때는 갈려도 되는지 먼저 정한다

토글은 `<details>` / `<summary>`이고 자식을 탭으로 들여쓴다. 계층은 `5.3.N.M`이 탭 2개, `a)`가 3개, 그 아래가 4개, 항목이 5개다. **페이지 A에서는 `5.1.6.N`이 그 자리였다.**
